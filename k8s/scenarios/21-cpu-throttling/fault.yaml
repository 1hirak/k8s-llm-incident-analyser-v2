apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo
spec:
  template:
    spec:
      containers:
      - name: demo-app
        resources:
          requests:
            cpu: "1m"
          limits:
            cpu: "1m"
