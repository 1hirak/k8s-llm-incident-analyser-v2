apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo
spec:
  template:
    spec:
      containers:
      - name: demo-app
        startupProbe:
          httpGet:
            path: /startup
            port: 8000
          failureThreshold: 3
          periodSeconds: 3
