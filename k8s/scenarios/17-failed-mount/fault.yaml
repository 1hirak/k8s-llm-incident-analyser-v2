apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo
spec:
  template:
    spec:
      containers:
      - name: demo-app
        volumeMounts:
        - name: missing-data-volume
          mountPath: /var/lib/demo
