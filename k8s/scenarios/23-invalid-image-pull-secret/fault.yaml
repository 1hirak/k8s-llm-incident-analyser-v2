apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo
spec:
  template:
    spec:
      imagePullSecrets:
      - name: missing-registry-secret
      containers:
      - name: demo-app
        image: demo-app:latest
        imagePullPolicy: Always
