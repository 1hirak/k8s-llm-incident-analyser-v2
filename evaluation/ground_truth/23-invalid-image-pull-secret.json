{
  "scenario_id": "23-invalid-image-pull-secret",
  "name": "Invalid Image Pull Secret",
  "description": "Container uses an image pull secret that does not exist and forces a registry pull",
  "true_root_cause": "Kubernetes cannot retrieve the configured image pull secret or image",
  "true_affected_component": "demo-app",
  "true_failure_category": "image",
  "true_severity": "critical",
  "expected_log_patterns": ["FailedToRetrieveImagePullSecret", "ErrImagePull", "ImagePullBackOff"],
  "expected_event_reasons": ["Failed", "BackOff"],
  "correct_remediation_keywords": ["imagePullSecrets", "Secret", "registry", "image"],
  "notes": "The scenario also forces imagePullPolicy=Always so the missing credential is exercised."
}
