{
  "scenario_id": "24-missing-service-account",
  "name": "Missing Service Account",
  "description": "Pod references missing-demo-service-account, which is not present in the namespace",
  "true_root_cause": "The deployment references a ServiceAccount that does not exist",
  "true_affected_component": "demo-app",
  "true_failure_category": "config",
  "true_severity": "high",
  "expected_log_patterns": ["serviceaccount", "not found", "CreateContainerConfigError"],
  "expected_event_reasons": ["Failed", "BackOff"],
  "correct_remediation_keywords": ["ServiceAccount", "serviceAccountName", "create", "deployment"],
  "notes": "The pod cannot start until the reference is corrected or the account is created."
}
